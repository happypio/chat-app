# [START django_app]
service: default
runtime: python310
env: standard
entrypoint: gunicorn -b :$PORT chat_app.wsgi

handlers:
  - url: /static
    static_dir: static/
  - url: /.*
    script: auto

runtime_config:
  python_version: 3